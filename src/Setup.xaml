<UserControl x:Class="FPing_V2.Setup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FPing_V2" 
    mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#120A1F">

    <UserControl.Resources>
        <local:ColorBrightnessConverter x:Key="ColorBrightnessConverter"/>

        <SolidColorBrush x:Key="PrimaryBackground" Color="#120A1F"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#2C2D43"/>
        <SolidColorBrush x:Key="AccentColor" Color="#5E4B8B"/>
        <SolidColorBrush x:Key="HoverAccentColor" Color="#7A60B3"/>
        <SolidColorBrush x:Key="PressedAccentColor" Color="#4D3D73"/>
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubTextColor" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="ButtonBorderColor" Color="#50426F"/>
        <SolidColorBrush x:Key="CloseButtonHover" Color="#E81123"/>
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="SubtitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{StaticResource SubTextColor}"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>

        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush>
                                            <SolidColorBrush.Color>
                                                <MultiBinding Converter="{StaticResource ColorBrightnessConverter}">
                                                    <Binding RelativeSource="{RelativeSource Mode=TemplatedParent}" Path="Background.Color"/>
                                                    <Binding Source="1.2"/>
                                                </MultiBinding>
                                            </SolidColorBrush.Color>
                                        </SolidColorBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush>
                                            <SolidColorBrush.Color>
                                                <MultiBinding Converter="{StaticResource ColorBrightnessConverter}">
                                                    <Binding RelativeSource="{RelativeSource Mode=TemplatedParent}" Path="Background.Color"/>
                                                    <Binding Source="0.8"/>
                                                </MultiBinding>
                                            </SolidColorBrush.Color>
                                        </SolidColorBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NextButtonStyle" TargetType="Button" BasedOn="{StaticResource CustomButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource CustomButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderColor}"/>
        </Style>

        <Style x:Key="NameInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="IBeam"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <TextBlock Text="Tell us a bit about yourself" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock Text="Nice to meet you! What's your name?" Style="{StaticResource SubtitleTextStyle}"/>

            <Border Background="{StaticResource SecondaryBackground}"
                    CornerRadius="10"
                    Padding="10"
                    Height="80"
                    Margin="0,0,0,40">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Source="pack://application:,,,/public/assets/setup/character_icon.png"
                           Width="60" Height="60" Margin="0,0,10,0" VerticalAlignment="Center"/>

                    <TextBox Grid.Column="1"
                             x:Name="NameTextBox"
                             Style="{StaticResource NameInputStyle}"
                             Text="Hi! I'm..."
                             GotFocus="NameTextBox_GotFocus"
                             LostFocus="NameTextBox_LostFocus"
                             TextChanged="NameTextBox_TextChanged"/>
                </Grid>
            </Border>
        </StackPanel>

        <Grid Grid.Row="3" HorizontalAlignment="Right" Margin="0,0,40,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Back" Style="{StaticResource BackButtonStyle}" Margin="0,0,10,0" Click="Back_Click"/>
                <Button x:Name="NextButton" Content="Next" Style="{StaticResource NextButtonStyle}" IsEnabled="False" Click="Next_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>